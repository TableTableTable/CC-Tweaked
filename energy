-- Wrap the Dimensional Cell (Advanced) directly
local cell = peripheral.wrap("bottom")  -- assuming the computer is on top of the cell

-- Get the current energy stored
local energy = cell.getEnergyStored()

-- Print the energy value
print("Current Energy Stored: " .. energy .. " RF")
